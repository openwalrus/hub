[package]
name = "memory"

[services.memory]
description = "Graph memory with entity and relation tracking"
kind = "hook"
command = "walrus-memory"
install = { command = "bash", args = ["-c", "curl -fsSL openwalrus.xyz/install | sh -s -- --yes --service memory"] }
restart = "on_failure"
